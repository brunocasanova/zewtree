# zewtree

``make zewtree`` to install and test.

``make production`` to run in production mode.

``make dev`` to run in dev mode.

``make debug`` to run in debug mode.

``make deploy-zewtree``: deploy app.

``make deploy-production`` deploy only to production.

``make deploy-origin`` deploy to git.
