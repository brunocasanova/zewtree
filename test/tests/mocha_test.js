var assert = require( 'assert' );

describe( 'mocha', function(){  
	
	it( "should fail when throwing an error" );

	it( "should fail when asserting false" );

	it( "should pass when finishing without error" );

	it( "should pass when asserting true" );

	it( "should be pending with no function" );

});